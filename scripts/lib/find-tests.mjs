// ═══════════════════════════════════════════════════════════════════════════
// ΠΟΥ ΕΙΝΑΙ ΟΙ ΣΟΥΙΤΕΣ — ΜΙΑ ΑΠΑΝΤΗΣΗ, ΟΧΙ ΔΥΟ
// ─────────────────────────────────────────────────────────────────────────
// ΤΟ ΣΦΑΛΜΑ, ΜΕΤΡΗΜΕΝΟ. Δύο εργαλεία έψαχναν σουίτες, το καθένα με τη δική του
// λίστα φακέλων:
//
//     scripts/test.mjs            ROOTS = ['lib', 'app', 'supabase']
//     scripts/guard-test-tail.mjs ROOTS = ['lib', 'app', 'components', 'supabase']
//
// Η διαφορά είναι μία λέξη, και κόστισε μια ολόκληρη σουίτα: το
// `components/confirmBus.test.ts` — ο δίαυλος επιβεβαίωσης που στέκεται μπροστά
// από εικοσιπέντε οριστικές διαγραφές — ΔΕΝ έτρεχε ποτέ στο CI. Ο φύλακας της
// ουράς το διάβαζε, ο εκτελεστής όχι.
//
// Είναι ακριβώς το σφάλμα που το `test.mjs` γράφτηκε για να εξαλείψει: η
// επικεφαλίδα του λέει ότι αντικατέστησε μια χειρόγραφη λίστα που είχε χάσει 25
// από 63 αρχεία. Η αυτόματη ανακάλυψη λύνει το πρόβλημα ΜΟΝΟ αν δεν κουβαλά κι
// αυτή χειρόγραφη λίστα — και η λίστα φακέλων ήταν ακριβώς αυτό.
//
// Η ΛΥΣΗ ΕΙΝΑΙ ΤΟ ΑΝΤΙΣΤΡΟΦΟ ΕΡΩΤΗΜΑ. Δεν απαριθμούμε πού ΜΠΟΡΕΙ να υπάρχει
// σουίτα — δεν το ξέρουμε, και κάθε νέος φάκελος το αλλάζει σιωπηλά. Σαρώνουμε
// ολόκληρο το repo και εξαιρούμε ρητά ό,τι δεν είναι πηγαίος κώδικας. Ένας νέος
// φάκελος συμπεριλαμβάνεται από μόνος του· μια σουίτα δεν μπορεί να ορφανέψει.
// ═══════════════════════════════════════════════════════════════════════════
import { readdirSync, statSync, existsSync } from 'node:fs'
import { join } from 'node:path'

/** Ό,τι δεν είναι πηγαίος κώδικας του έργου. Ρητά, και σε ΕΝΑ σημείο. */
const SKIP = new Set(['node_modules', '.next', '.git', '.vscode', 'public', 'coverage'])

/**
 * ΤΑ ΠΡΟΠΛΑΣΜΑΤΑ ΔΕΝ ΕΙΝΑΙ ΚΩΔΙΚΑΣ ΤΟΥ ΠΡΟΪΟΝΤΟΣ, ΚΑΙ ΕΙΝΑΙ ΓΕΜΑΤΑ ΣΦΑΛΜΑΤΑ.
 *
 * Ο κατάλογος μεταλλάξεων (`scripts/lib/mutations.mjs`) περιέχει, εξ ορισμού,
 * μία παράβαση για κάθε φύλακα: αυτός είναι ο σκοπός του. Χωρίς εξαίρεση, ο
 * πρώτος φύλακας που σαρώνει το `scripts/` κοκκινίζει πάνω στο ίδιο το αρχείο
 * που αποδεικνύει ότι δουλεύει, και ο πάγκος δεν μπορεί καν να ξεκινήσει.
 *
 * Το κριτήριο ζει ΕΔΩ και όχι σε οκτώ φύλακες: όποιο πρόπλασμα προστεθεί
 * αύριο, θα εξαιρεθεί από όλους μαζί.
 */
export const isFixture = (path) => /(^|\/)mutations\.mjs$/.test(path)

/**
 * Κάθε `*.test.ts` του αποθετηρίου, ταξινομημένο.
 * @param {string} root ρίζα σάρωσης (προεπιλογή: ο τρέχων φάκελος)
 */
export function findTests(root = '.') {
  const out = []
  walk(root, out)
  out.sort()
  return out
}

/**
 * Κάθε πηγαίο `.ts`/`.tsx` του αποθετηρίου — για φύλακες που σαρώνουν κώδικα,
 * όχι σουίτες. Ίδια σάρωση, ίδιες εξαιρέσεις, ένα σημείο.
 */
export function findSources(root = '.') {
  const out = []
  walk(root, out, e => /\.tsx?$/.test(e) && !e.endsWith('.d.ts'))
  out.sort()
  return out
}

function walk(dir, out, keep = e => e.endsWith('.test.ts')) {
  if (!existsSync(dir)) return
  for (const entry of readdirSync(dir)) {
    if (SKIP.has(entry)) continue
    const full = join(dir, entry)
    if (isFixture(full)) continue
    let st
    try { st = statSync(full) } catch { continue }   // σύνδεσμος που δείχνει στο πουθενά
    if (st.isDirectory()) walk(full, out, keep)
    else if (keep(entry)) out.push(full.replace(/^\.\//, ''))
  }
}
