# ═══════════════════════════════════════════════════════════════════════════
#  ΟΙ ΜΕΤΑΝΑΣΤΕΥΣΕΙΣ ΠΟΥ ΔΕΝ ΑΝΤΕΧΟΥΝ ΔΕΥΤΕΡΗ ΕΚΤΕΛΕΣΗ, ΚΑΙ ΓΙΑΤΙ ΜΕΝΟΥΝ
# ─────────────────────────────────────────────────────────────────────────
#  Το scripts/db-replay.sh κλωνοποιεί τη βάση στην τελική της κατάσταση και
#  ξανατρέχει ΚΑΘΕ μετανάστευση μόνη της. Όποια σκάσει και δεν είναι γραμμένη
#  εδώ, γίνεται κόκκινο: είναι ΝΕΟ χρέος.
#
#  ΓΙΑΤΙ ΔΕΝ ΜΠΑΛΩΝΟΝΤΑΙ ΟΙ ΠΑΡΑΚΑΤΩ. Καθεμία σκάει επειδή μια ΜΕΤΕΠΕΙΤΑ
#  μετανάστευση άλλαξε τον κόσμο κάτω από τα πόδια της: έσβησε τη συνάρτηση,
#  άλλαξε τον τύπο επιστροφής, μετονόμασε τη στήλη, γύρισε το text σε uuid. Το
#  σφάλμα ΕΙΝΑΙ η προστασία. Αν τους βάζαμε φύλαξη ώστε να περνούν, θα
#  περνούσαν γράφοντας ΠΑΝΩ στη σημερινή κατάσταση την παλιά τους εκδοχή και
#  θα έχανε το repo δουλειά που έγινε αργότερα. Το μετρήσαμε: η
#  `get_accountant_data` έχανε την απαίτηση συνδρομής της πύλης λογιστή.
#
#  ΤΙ ΕΠΙΤΡΕΠΕΤΑΙ ΝΑ ΑΛΛΑΞΕΙ ΕΔΩ. Μόνο αφαίρεση. Η προσθήκη ονόματος σημαίνει
#  ότι κάποιος έγραψε μετανάστευση που δεν ξανατρέχει και αυτό ακριβώς
#  σταματά ο έλεγχος.
# ═══════════════════════════════════════════════════════════════════════════

# ERROR: cannot change return type of existing function
# Το θεμέλιο. Χτίζει από το μηδέν σε άδεια βάση, εξ ορισμού μία φορά.
00000000000000_baseline.sql

# ERROR: function public.is_active_portal_token(text) does not exist
# Το 20260804200000 γύρισε το διακριτικό σε uuid και έσβησε την παλιά υπογραφή.
00000000000001_platform_storage.sql

# ERROR: operator does not exist: text = uuid
# Η ίδια η μετανάστευση άλλαξε τον τύπο της στήλης· η δεύτερη εκτέλεση βρίσκει
# uuid εκεί που ο κώδικάς της περιμένει text.
20260804200000_portal_link_tenant_binding.sql

# ERROR: cannot change return type of existing function
# Η `get_accountant_data` ξαναγράφτηκε αργότερα με άλλο σχήμα επιστροφής.
20260805070000_accountant_portal_and_account_deletion.sql

# ERROR: η bump_ai_usage πρέπει να έχει ΜΙΑ υπογραφή, βρέθηκαν 2
# Ο έλεγχος είναι ΜΕΣΑ στη μετανάστευση και μετρά σωστά: στην τελική βάση
# υπάρχουν δύο υπογραφές, γιατί μεταγενέστερη μετανάστευση πρόσθεσε τη δεύτερη.
20260808000000_plan_max_properties_search_path.sql

# ERROR: function public.org_owner_ids(uuid) does not exist
# Η συνάρτηση μετακόμισε στο σχήμα `private` σε μεταγενέστερη μετανάστευση.
20260810060000_parent_property_scope.sql

# ERROR: policy "own_billing_profile_read" for table "billing_profiles" already exists
# Οι τρεις πολιτικές θα μπορούσαν να φυλαχθούν, αλλά το ίδιο αρχείο ορίζει και
# την `lock_billing_plan`, που ξαναγράφτηκε δύο φορές αργότερα. Μια δεύτερη
# εκτέλεση θα περνούσε καθαρή γυρίζοντας πίσω αυτές τις δύο διορθώσεις.
20260814020000_security_and_integrity_fixes.sql

# ERROR: operator does not exist: text = uuid
# Στήλες που η ίδια γύρισε από text σε uuid.
20260814090000_referential_integrity.sql

# ERROR: function public.purge_property_children() does not exist
# Η σκανδάλη μετακόμισε στο σχήμα `private` αργότερα.
20260815080000_purge_trigger_least_privilege.sql

# ERROR: column "stripe_customer_id" does not exist
# Μετονομασία. Η στήλη λέγεται πια `mor_customer_id`, άρα η πηγή της
# μετονομασίας δεν υπάρχει. Και το αρχείο ορίζει την `lock_billing_plan`, που
# ξαναγράφτηκε αργότερα.
20260820120000_o_emporos_den_legetai_stripe.sql
