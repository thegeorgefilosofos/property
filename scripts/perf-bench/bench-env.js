// ═══════════════════════════════════════════════════════════════════════════
// ΤΟ `process.env` ΤΟΥ ΠΑΓΚΟΥ
// ─────────────────────────────────────────────────────────────────────────
// Στον περιηγητή δεν υπάρχει `process`. Οποιο module διαβάσει `process.env.X`
// για ένα X που δεν είναι ρητά δηλωμένο στο `define` ρίχνει ΟΛΗ τη σκηνή· και
// μια κενή σκηνή περνά κάθε έλεγχο διάταξης χωρίς εύρημα.
//
// Το κενό αντικείμενο δίνει `undefined` σε ό,τι δεν ξέρουμε, που είναι ακριβώς
// η συμπεριφορά μιας μεταβλητής περιβάλλοντος που δεν έχει τεθεί.
// ═══════════════════════════════════════════════════════════════════════════
export const BENCH_ENV = {};
