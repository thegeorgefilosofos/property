#!/usr/bin/env node
// ═══════════════════════════════════════════════════════════════════════════
// ΔΥΟ ΚΕΝΕΣ ΚΑΤΑΣΤΑΣΕΙΣ, ΔΥΟ ΔΙΑΤΥΠΩΣΕΙΣ. ΟΧΙ ΠΕΝΤΕ.
//
// Η ίδια οθόνη έλεγε το ίδιο πράγμα με πέντε τρόπους: «Δεν υπάρχουν επαφές»,
// «Καμία επαφή με αυτά τα κριτήρια», «Δεν υπάρχει ιστορικό ακόμα», «Το αρχείο
// του ακινήτου είναι κενό», «Δεν βρέθηκαν αποτελέσματα». Ο χρήστης δεν
// διαβάζει πέντε φορές· διαβάζει μία και μαθαίνει το σχήμα. Αν το σχήμα
// αλλάζει σε κάθε καρτέλα, δεν το μαθαίνει ποτέ και κάθε κενή οθόνη τον
// κάνει να αναρωτηθεί αν κάτι χάλασε.
//
// ΟΙ ΔΥΟ ΚΑΤΑΣΤΑΣΕΙΣ ΔΕΝ ΕΙΝΑΙ Η ΙΔΙΑ:
//
//   Δεν έχει καταχωρηθεί τίποτα      →  «Καμία δαπάνη ακόμη»
//   Το φίλτρο δεν βρήκε τίποτα       →  «Δεν βρέθηκαν δαπάνες»
//
// Το «ακόμη» λέει ότι υπάρχει χρόνος μπροστά και ότι φταίει η απουσία
// καταχώρησης. Σε αποτέλεσμα αναζήτησης είναι ψέμα: τα δεδομένα υπάρχουν,
// απλώς δεν ταιριάζουν. Γι' αυτό η δεύτερη μορφή δεν το παίρνει ΠΟΤΕ.
//
// ΤΙ ΑΠΑΓΟΡΕΥΕΤΑΙ ΡΗΤΑ
//   · «Δεν υπάρχ…» σε τίτλο κενής κατάστασης. Είναι η αόριστη μορφή που
//     γεννά τις υπόλοιπες τέσσερις.
//   · «ακόμα». Η εφαρμογή γράφει «ακόμη» σε 181 σημεία και «ακόμα» σε 9.
//     Δύο ορθογραφίες της ίδιας λέξης δεν είναι ύφος, είναι απροσεξία.
//   · Υπότιτλος που επαναλαμβάνει τον τίτλο. «Χαρτοφυλάκιο» με υπότιτλο
//     «Συγκεντρωτική εικόνα του χαρτοφυλακίου», «Λογαριασμός» με υπότιτλο
//     «Λογαριασμός, συνδρομή και προτιμήσεις». Ο υπότιτλος έχει μία δουλειά:
//     να πει τι κάνει η οθόνη που δεν το λέει ήδη ο τίτλος. Όταν επαναλαμβάνει,
//     καταλαμβάνει τη μοναδική γραμμή που θα το εξηγούσε.
//
// ΔΕΝ ΤΟΝ ΕΤΡΕΧΕ ΚΑΝΕΝΑ WORKFLOW. Ο φύλακας ήταν γραμμένος και δηλωμένος στο
// package.json και κανένα workflow δεν τον καλούσε. Ενας φύλακας που δεν
// τρέχει δεν φυλάει τίποτα, απλώς δίνει την εντύπωση ότι φυλάει. Στην ίδια
// κατάσταση βρέθηκαν τρεις φύλακες μαζί και το ίδιο είχε συμβεί με τη σουίτα
// του confirmBus, που ήταν γραμμένη και δεν την έβρισκε ο εκτελεστής.
// ═══════════════════════════════════════════════════════════════════════════
import { readFileSync } from 'node:fs';
import { globSync } from 'node:fs';

const FILES = globSync(['app/**/*.tsx', 'app/**/*.ts', 'components/**/*.tsx', 'lib/**/*.ts'])
  .filter(f => !f.endsWith('.test.ts') && !f.includes('/node_modules/'));

/** Τίτλοι κενών καταστάσεων που δεν περιγράφουν απουσία — δεν τους αφορά ο κανόνας. */
const NOT_ABSENCE = /^(Ξεκίνα|Όλα |Όλες |Δεν εκκρεμεί|Συμπλήρωσε|Έλεγχος)/;

const badTitle = [];
const badSpelling = [];
const badEnglish = [];
const badLabel = [];

// ── ΤΑ ΕΛΛΗΝΙΚΑ ΔΕΝ ΕΧΟΥΝ ΤΙΤΛΟΠΟΙΗΣΗ ────────────────────────────────────
// «Έτος Κατασκευής», «Τύπος Θέρμανσης», «Ημερομηνία Λήξης»: κάθε λέξη με
// κεφαλαίο, όπως στα αγγλικά. Η μισή εφαρμογή έγραφε έτσι και η άλλη μισή
// κανονικά, οπότε δύο πεδία δίπλα δίπλα φαίνονταν να ανήκουν σε άλλα προϊόντα.
//
// ΔΥΟ ΑΚΟΜΗ ΚΑΝΟΝΕΣ ΤΗΣ ΙΔΙΑΣ ΟΙΚΟΓΕΝΕΙΑΣ:
//   · Η κάθετος δεν είναι ελληνικός ΣΥΝΔΕΣΜΟΣ. «Τεχνικός / Εταιρεία» δεν λέει
//     αν θέλει και τα δύο ή το ένα. Εκεί, στα ελληνικά, μπαίνει «ή».
//
//     ΕΙΝΑΙ ΟΜΩΣ ΔΙΑΙΡΕΤΙΚΗ, ΚΑΙ ΑΥΤΟ ΕΠΙΤΡΕΠΕΤΑΙ. «Κόστος / Επίσκεψη», «Χημικά
//     / Μήνα», «€ / kWh»: εδώ η κάθετος δεν ενώνει δύο εναλλακτικές, διαιρεί ένα
//     μέγεθος με μια μονάδα — ίδια ανάγνωση με τα τιμολόγια και τα παραστατικά
//     που κρατά ο ίδιος ο χρήστης. Ο κανόνας ξεχωρίζει τα δύο από το ΤΙ στέκεται
//     εκατέρωθεν: μέγεθος αριστερά και μονάδα δεξιά περνά, οτιδήποτε άλλο όχι.
//   · Η μονάδα λέγεται ΜΙΑ φορά. «Μηνιαίο κόστος (€)» με «€» κολλητά στο
//     πλαίσιο είναι το ίδιο σύμβολο δύο φορές, δώδεκα εικονοστοιχεία μακριά.
const GR_UP = 'ΑΒΓΔΕΖΗΘΙΚΛΜΝΞΟΠΡΣΤΥΦΧΨΩΆΈΉΊΌΎΏ';
const GR_LO = 'αβγδεζηθικλμνξοπρστυφχψωάέήίόύώς';
/** Επίσημες ονομασίες θεσμών και εντύπων: γράφονται έτσι στα ελληνικά. */
const PROPER = /Ενιαίος Φόρος|Αριθμός Φορολογικού|Ανεξάρτητη Αρχή|Φόρος Προστιθέμενης|Ειδικός Φόρος|Αριθμός Μητρώου|Δήλωση Μίσθωσης|Ψηφιακό Τέλος|Πολιτική Απορρήτου|Όροι Χρήσης/;
// Το `placeholder` εξαιρείται: δείχνει ΠΑΡΑΔΕΙΓΜΑ τιμής («Γιώργος Παπαδόπουλος»,
// «Canva Pro») και τα ονόματα ανθρώπων και εταιρειών κρατούν τα κεφαλαία τους.
const TITLE_CASE = new RegExp(`(?:label|title)="([${GR_UP}][${GR_LO}]+(?: [${GR_UP}][${GR_LO}]+)+)"`, 'g');
const SLASH = new RegExp(`(?:label|title|placeholder)="([^"]*[${GR_LO}${GR_UP}][^"]*\\s/\\s[^"]*)"`, 'g');
/**
 * Η ΔΙΑΙΡΕΤΙΚΗ ΚΑΘΕΤΟΣ: μέγεθος αριστερά, μονάδα δεξιά.
 *
 * Κλειστοί κατάλογοι και στις δύο πλευρές, επίτηδες. Ένας κανόνας «ό,τι έχει
 * κάθετο με κενά είναι ρυθμός» θα ξανάνοιγε την πόρτα στο «Τεχνικός / Εταιρεία»,
 * που είναι ακριβώς αυτό που πιάνει ο φύλακας.
 */
const RATE = new RegExp(
  `^(?:Κόστος|Τιμή|Χρέωση|Πάγιο|Δαπάνη|Έσοδο|Κατανάλωση|Χημικά|Αμοιβή)[^/]*`
  + ` / (?:Μονάδα|Επίσκεψη|Μήνα|Έτος|Ημέρα|Ώρα|Άτομο|Τεμάχιο|Δωμάτιο|Απεντόμωση|Τετραγωνικό)$`);
const UNIT_TWICE = /label="[^"]*\((?:€|kWh|μήνες|%|τ\.μ\.|€\/MWh)\)"/g;

// ── Ξένες λέξεις που έχουν ελληνικό όνομα στην εφαρμογή ───────────────────
// Το «Email» ήταν σε είκοσι πέντε σημεία, δίπλα σε «Τηλέφωνο» και «Ονοματεπώνυμο».
// Δεν είναι μάρκα όπως το WhatsApp ή το Viber: έχει ελληνική λέξη και η οθόνη
// που μιλά ελληνικά παντού εκτός από ένα πεδίο δεν διαβάζεται ως επιμελημένη.
const GR = 'ΑΒΓΔΕΖΗΘΙΚΛΜΝΞΟΠΡΣΤΥΦΧΨΩάέήίόύώϊϋΐΰαβγδεζηθικλμνξοπρστυφχψως';
/** Έκθλιψη ή αριθμητικός δείκτης γραμμένος με ίσιο ASCII απόστροφο. */
const ASCII_APOSTROPHE = new RegExp(`[${GR}]'(?= ?[${GR}])`);

const ENGLISH = [
  [/(?:^|[>"'\s])Email(?:[<"'\s.,:]|$)/, '«Email» → «Ηλεκτρονικό ταχυδρομείο»'],
  // Η ΛΕΞΗ ΤΗΣ ΟΜΑΔΑΣ ΔΕΝ ΕΙΝΑΙ ΛΕΞΗ ΤΟΥ ΠΡΟΪΟΝΤΟΣ. Το «ντοσιέ» είναι το
  // εσωτερικό μας όνομα για το πλαϊνό πάνελ — δεκαεπτά φορές σε σχόλια και
  // σωστά. Είχε όμως διαρρεύσει και σε υπότιτλο οθόνης («με πλήρες ντοσιέ»),
  // όπου ο ιδιοκτήτης διαβάζει μια γαλλική δάνεια λέξη γραφείου αντί για τον
  // όρο που χρησιμοποιεί η ίδια η εφαρμογή παντού αλλού: «φάκελος».
  // Ο κανόνας κοιτά ΜΟΝΟ ορατό κείμενο· τα σχόλια εξαιρούνται πιο κάτω.
  [/ντοσιέ/, '«ντοσιέ» → «φάκελος»: εσωτερική λέξη, όχι λέξη του χρήστη'],
];

/**
 * Τα σχόλια ΔΕΝ είναι κείμενο οθόνης και τα σχόλια JSX (`{/* … *\/}`) δεν
 * πιάνονται από τον έλεγχο «η γραμμή αρχίζει με //». Ένα σχόλιο πέντε γραμμών
 * που εξηγεί ΓΙΑΤΙ αποφύγαμε μια λέξη θα κατηγορούνταν ότι τη χρησιμοποιεί.
 * Οι γραμμές μέσα σε μπλοκ σχολίου αντικαθίστανται με κενές, ώστε οι αριθμοί
 * γραμμών να μένουν σωστοί στα μηνύματα.
 */
function withoutBlockComments(text) {
  const out = [];
  let inBlock = false;
  for (const line of text.split('\n')) {
    const opens = /\{?\/\*/.test(line);
    const closes = /\*\/\}?/.test(line);
    if (inBlock) {
      out.push('');
      if (closes) inBlock = false;
      continue;
    }
    if (opens && !closes) { inBlock = true; out.push(''); continue; }
    if (opens && closes) { out.push(line.replace(/\{?\/\*[\s\S]*?\*\/\}?/g, '')); continue; }
    out.push(line);
  }
  return out;
}

for (const file of FILES) {
  const src = readFileSync(file, 'utf8');
  const lines = withoutBlockComments(src);

  // ── Κανόνας 1: «ακόμα» πουθενά ─────────────────────────────────────────
  // ΗΤΑΝ ΓΡΑΜΜΕΝΟΣ /\bακόμα\b/ ΚΑΙ ΔΕΝ ΕΨΑΧΝΕ ΤΙΠΟΤΑ. Το «\b» της JavaScript
  // ορίζεται πάνω στο ASCII \w, οπότε δίπλα σε ελληνικό γράμμα δεν ταιριάζει
  // ΠΟΤΕ — ούτε καν στη σκέτη συμβολοσειρά «ακόμα». Ο κανόνας που διαφημιζόταν
  // στην κεφαλίδα αυτού του αρχείου ήταν, κυριολεκτικά, ανενεργός.
  lines.forEach((line, i) => {
    const t = line.trim();
    if (t.startsWith('//') || t.startsWith('*')) return;   // σχόλιο, όχι κείμενο οθόνης
    if (/(?<![\p{L}\p{N}])ακόμα(?![\p{L}\p{N}])/u.test(line)) badSpelling.push(`${file}:${i + 1}  ${t.slice(0, 90)}`);
  });

  // ── Κανόνας 2: καμία αγγλική λέξη που έχει ελληνικό όνομα ─────────────
  lines.forEach((line, i) => {
    const t = line.trim();
    if (t.startsWith('//') || t.startsWith('*')) return;
    for (const [re, msg] of ENGLISH) {
      if (re.test(line)) badEnglish.push(`${file}:${i + 1}  ${msg}  ${t.slice(0, 70)}`);
    }
  });

  // ── Κανόνας 2β: η απόστροφος δεν είναι εισαγωγικό ─────────────────────
  // Η έκθλιψη στα ελληνικά γράφεται με τυπογραφική απόστροφο «’» (U+2019), όχι
  // με το ίσιο ASCII «'» του πληκτρολογίου: «γράψ’ την», «γι’ αυτό». Και ο
  // αριθμητικός δείκτης θέλει ΚΕΡΑΙΑ «΄» (U+0374): «ΔΟΥ Α΄ Αθηνών», όχι «Α'».
  //
  // Βρέθηκαν έντεκα σημεία σε ένδεκα αρχεία, ανάμεσά τους η ίδια η ταυτότητα της
  // Νόα και οι Επαφές. Είναι από τις λεπτομέρειες που κανείς δεν ονομάζει, αλλά
  // ξεχωρίζουν ένα ελληνικό προϊόν από ένα μεταφρασμένο.
  //
  // Ο έλεγχος κοιτά ΜΟΝΟ μέσα σε συμβολοσειρές με διπλά εισαγωγικά ή backtick —
  // εκεί μπορεί να ζήσει απόστροφος — και μόνο όταν έχει ελληνικό γράμμα πριν
  // και ελληνικό γράμμα μετά (με ή χωρίς κενό). Έτσι το `'κείμενο'` ως όριο
  // συμβολοσειράς και τα ternary μέσα σε `${…}` δεν μετρούν.
  lines.forEach((line, i) => {
    const t = line.trim();
    if (t.startsWith('//') || t.startsWith('*')) return;
    for (const m of line.matchAll(/"([^"\n]*)"|`([^`\n]*)`/g)) {
      const raw = m[1] ?? m[2] ?? '';
      const clean = raw.replace(/\$\{[^}]*\}/g, '§');
      if (ASCII_APOSTROPHE.test(clean)) {
        badLabel.push(`${file}:${i + 1}  ίσια απόστροφος: «${clean.slice(0, 46)}» → χρησιμοποίησε ’ (ή ΄ για αριθμητικό δείκτη)`);
      }
    }
  });

  // ── Κανόνας 3: ετικέτες πεδίων ─────────────────────────────────────────
  lines.forEach((line, i) => {
    const t = line.trim();
    if (t.startsWith('//') || t.startsWith('*')) return;
    for (const m of line.matchAll(TITLE_CASE)) {
      if (PROPER.test(m[1])) continue;
      badLabel.push(`${file}:${i + 1}  τιτλοποίηση: «${m[1]}»`);
    }
    for (const m of line.matchAll(SLASH)) {
      if (RATE.test(m[1])) continue;   // ρυθμός, όχι εναλλακτική
      badLabel.push(`${file}:${i + 1}  κάθετος: «${m[1]}» → «ή» για επιλογή, ή μέγεθος / μονάδα`);
    }
    for (const m of line.matchAll(UNIT_TWICE)) {
      if (!/suffix=/.test(line)) continue;
      badLabel.push(`${file}:${i + 1}  μονάδα δύο φορές: ${m[0].slice(0, 48)}`);
    }
  });

  // ── Κανόνας 4: κανένας τίτλος EmptyState δεν ξεκινά με «Δεν υπάρχ» ─────
  for (const m of src.matchAll(/<EmptyState[\s\S]{0,500}?\/>/g)) {
    const t = m[0].match(/title=\{?[`"']([^`"']+)/);
    if (!t) continue;
    const title = t[1].trim();
    if (NOT_ABSENCE.test(title)) continue;
    const ln = src.slice(0, m.index).split('\n').length;
    if (/^Δεν υπάρχ/.test(title)) {
      badTitle.push(`${file}:${ln}  «${title}»`);
      continue;
    }
    // «Δεν βρέθηκαν …» δεν παίρνει ποτέ «ακόμη»: τα δεδομένα ΥΠΑΡΧΟΥΝ.
    if (/^Δεν βρέθηκ/.test(title) && /ακόμη/.test(title)) {
      badTitle.push(`${file}:${ln}  «${title}» (αποτέλεσμα φίλτρου δεν παίρνει «ακόμη»)`);
    }
  }
}

// ── Η ΑΟΡΙΣΤΗ ΜΟΡΦΗ, ΟΠΟΥ ΚΙ ΑΝ ΓΡΑΦΤΕΙ ───────────────────────────────────
// Ο έλεγχος του τίτλου κοίταζε ΜΟΝΟ τα props του κοινού <EmptyState>. Δύο κενές
// καταστάσεις όμως ήταν γραμμένες με το χέρι — «Δεν υπάρχουν ακίνητα για αυτή
// τη χρονιά» στη σελίδα του λογιστή, «Δεν υπάρχουν σχόλια ακόμη» στους
// Επισκέπτες — και περνούσαν ανενόχλητες. Η απαγόρευση αφορά τη ΔΙΑΤΥΠΩΣΗ, όχι
// το ποιο συστατικό την αποδίδει.
//
// ΠΟΥ ΔΕΝ ΑΦΟΡΑ, ΚΑΙ ΓΙΑΤΙ. Πρώτη, πολύ πλατιά εκδοχή του ελέγχου έβγαλε 17
// ευρήματα και τα δεκατρία ήταν ΜΗΝΥΜΑΤΑ ΕΝΕΡΓΕΙΑΣ: «Δεν υπάρχουν δόσεις προς
// εξαγωγή» όταν πατάς Εξαγωγή χωρίς δόσεις. Εκεί η φράση είναι σωστή — εξηγεί
// γιατί μια ενέργεια δεν έκανε τίποτα, δεν τιτλοφορεί μια άδεια περιοχή. Η
// διάκριση γίνεται στον ΠΑΡΑΛΗΠΤΗ: κείμενο μέσα στην οθόνη, όχι όρισμα σε
// ειδοποίηση. Έλεγχος με 17 ευρήματα από τα οποία 13 ψευδή δεν διαβάζεται.
const OFFLINE = 'app/offline/page.tsx';   // «Δεν υπάρχει σύνδεση»: κατάσταση δικτύου, όχι απουσία δεδομένων
const vague = [];
for (const f of FILES) {
  if (f === OFFLINE) continue;
  readFileSync(f, 'utf8').split('\n').forEach((line, i) => {
    const t = line.trim();
    if (t.startsWith('//') || t.startsWith('*')) return;
    // Ειδοποίηση, επιστροφή σφάλματος ή ρίψη: εξηγεί ενέργεια, δεν τιτλοφορεί κενό.
    if (/(notifyError|notifyOk|setErr|setError|set\w*Err|throw |alert\(|return\s+['"]|error:|reason:|message:)/.test(line)) return;
    // Μόνο κείμενο που ΑΠΟΔΙΔΕΤΑΙ μέσα στην οθόνη, ή τίτλος/υπόδειξη κενής κατάστασης.
    const m = line.match(/>(Δεν υπάρχ(?:ει|ουν)\s[^<{\n]{2,60})</)
           || line.match(/\b(?:title|hint|label)=["'](Δεν υπάρχ(?:ει|ουν)\s[^"'\n]{2,60})["']/);
    if (m) vague.push(`${f}:${i + 1}  «${m[1]}»`);
  });
}

// ── Υπότιτλος που επαναλαμβάνει τον τίτλο ────────────────────────────────
// Η σύγκριση γίνεται σε θέμα (χωρίς τόνους, χωρίς κατάληξη), αλλιώς το
// «Χαρτοφυλάκιο» και το «χαρτοφυλακίου» θα περνούσαν για διαφορετικές λέξεις.
const stemGr = w => w.normalize('NFD').replace(/[\u0300-\u036f]/g, '').toLowerCase()
  .replace(/(ς|ν|υ|α|ο|ε|η|ι|ων|ους|ου|ας|ες|οι)$/, '');
const echoedSub = [];
for (const f of FILES) {
  const src = readFileSync(f, 'utf8');
  for (const m of src.matchAll(/<PageTitle\s+title="([^"]+)"\s+sub="([^"]+)"/g)) {
    const t = new Set([...m[1].matchAll(/[Α-Ωα-ωΆ-Ώά-ώ]{4,}/g)].map(x => stemGr(x[0])));
    const echo = [...m[2].matchAll(/[Α-Ωα-ωΆ-Ώά-ώ]{4,}/g)].map(x => x[0]).filter(w => t.has(stemGr(w)));
    if (echo.length) echoedSub.push(`${f}: «${m[1]}» → «${m[2]}» (${echo.join(', ')})`);
  }
}

let failed = false;

if (vague.length) {
  failed = true;
  console.error(`✗ ${vague.length} φορές η αόριστη μορφή «Δεν υπάρχ…»:\n`);
  console.error('   Τίποτα καταχωρημένο →  «Κανένα σχόλιο ακόμη»');
  console.error('   Το φίλτρο δεν βρήκε →  «Δεν βρέθηκαν σχόλια»\n');
  for (const b of vague) console.error('   ' + b);
  console.error('');
}

if (echoedSub.length) {
  failed = true;
  console.error(`✗ ${echoedSub.length} υπότιτλοι επαναλαμβάνουν τον τίτλο τους.\n`);
  console.error('   Ο υπότιτλος λέει τι κάνει η οθόνη — όχι ξανά το όνομά της.\n');
  for (const b of echoedSub) console.error('   ' + b);
  console.error('');
}

if (badSpelling.length) {
  failed = true;
  console.error(`✗ «ακόμα» σε ${badSpelling.length} σημεία. Η εφαρμογή γράφει «ακόμη».\n`);
  for (const b of badSpelling) console.error('   ' + b);
  console.error('');
}

if (badTitle.length) {
  failed = true;
  console.error(`✗ ${badTitle.length} τίτλοι κενής κατάστασης εκτός κανόνα.\n`);
  console.error('   Τίποτα καταχωρημένο →  «Καμία δαπάνη ακόμη»');
  console.error('   Το φίλτρο δεν βρήκε →  «Δεν βρέθηκαν δαπάνες»\n');
  for (const b of badTitle) console.error('   ' + b);
  console.error('');
}

if (badEnglish.length) {
  failed = true;
  console.error(`✗ ${badEnglish.length} αγγλικές λέξεις που έχουν ελληνικό όνομα.\n`);
  for (const b of badEnglish) console.error('   ' + b);
  console.error('');
}

if (badLabel.length) {
  failed = true;
  console.error(`✗ ${badLabel.length} ετικέτες εκτός κανόνα.\n`);
  for (const b of badLabel) console.error('   ' + b);
  console.error('');
}

if (failed) process.exit(1);
console.log('✅ Λεκτικά: μία διατύπωση ανά κατάσταση, μία ορθογραφία για το «ακόμη», ελληνικά ονόματα.');
