// ═══════════════════════════════════════════════════════════════════════════
// ΠΟΥ ΕΠΙΤΡΕΠΕΤΑΙ ΝΑ ΣΤΕΙΛΕΙ ΤΟΝ ΧΡΗΣΤΗ ΕΝΑΣ ΣΥΝΔΕΣΜΟΣ ΠΟΥ ΤΟΝ ΓΡΑΦΕΙ ΑΛΛΟΣ
// ──────────────────────────────────────────────────────────────────────
// Η ανταλλαγή του διακριτικού (app/auth/callback) δέχεται προορισμό στη
// διεύθυνση, ώστε ο νέος λογαριασμός να προσγειώνεται στο ταμείο με το πακέτο
// που διάλεξε. Οποιος όμως διαβάζει προορισμό από τη διεύθυνση, τον διαβάζει
// από ΟΠΟΙΟΝΔΗΠΟΤΕ: ένας σύνδεσμος με τη δική μας υπογραφή που καταλήγει σε
// ξένη σελίδα εισόδου είναι το κλασικό ψάρεμα και δεν χρειάζεται τίποτα άλλο
// από μια ξεχασμένη γραμμή σαν κι αυτή.
//
// ── ΤΡΕΙΣ ΜΟΡΦΕΣ ΠΟΥ ΜΟΙΑΖΟΥΝ ΕΣΩΤΕΡΙΚΕΣ ΚΑΙ ΔΕΝ ΕΙΝΑΙ ──────────
//   «//kako.gr»  ο περιηγητής τη διαβάζει ως διεύθυνση άλλου τόπου.
//   «/\\kako.gr» η ανάποδη κάθετος κανονικοποιείται σε δεύτερη κάθετο.
//   «https://…»  προφανές και ακριβώς για αυτό το μόνο που συνήθως ελέγχεται.
//
// Ο κανόνας είναι θετικός, όχι κατάλογος απαγορεύσεων: δεκτή είναι ΜΟΝΟ
// σχετική διαδρομή που ξεκινά με μία κάθετο και δεν κρύβει δεύτερη από πίσω.
// ═══════════════════════════════════════════════════════════════════════════

/** Ο προορισμός όταν ο ζητούμενος δεν είναι δικός μας. */
export const HOME = '/dashboard';

export function safeNext(raw: string | null | undefined, fallback: string = HOME): string {
  const v = (raw || '').trim();
  if (!v.startsWith('/')) return fallback;
  // Δεύτερος χαρακτήρας κάθετος ή ανάποδη κάθετος: διεύθυνση άλλου τόπου.
  if (v[1] === '/' || v[1] === '\\') return fallback;
  // Χαρακτήρες ελέγχου στην κεφαλίδα «Location» σπάνε την απόκριση στα δύο.
  // eslint-disable-next-line no-control-regex
  if (/[\u0000-\u001f\u007f]/.test(v)) return fallback;
  return v;
}
