// ═══════════════════════════════════════════════════════════════════════════
// Καθαρισμός προσωπικών δεδομένων από τη ΣΥΣΚΕΥΗ
// ─────────────────────────────────────────────────────────────────────────
// Η εφαρμογή κρατά κάποια πράγματα τοπικά για ταχύτητα και άνεση. Μερικά από
// αυτά περιέχουν προσωπικά δεδομένα ΤΡΙΤΩΝ — ονόματα ενοικιαστών, ΑΦΜ
// συνιδιοκτητών, ποσά. Σε κοινόχρηστο υπολογιστή (γραφείο, σπίτι, internet
// café) δεν έχουν καμία δουλειά να επιβιώνουν της αποσύνδεσης.
//
// ΓΙΑΤΙ ΕΔΩ ΚΑΙ ΟΧΙ ΔΙΑΣΠΑΡΤΑ: όποιος προσθέτει νέο κλειδί localStorage με
// προσωπικά δεδομένα πρέπει να το γράψει σε ΕΝΑ σημείο. Ο προηγούμενος
// καθαρισμός έσβηνε μόνο τις συνομιλίες του βοηθού και άφηνε πίσω τα ΑΦΜ των
// συνιδιοκτητών — ακριβώς επειδή η λογική ήταν σκορπισμένη.
//
// ΤΙ ΔΕΝ ΣΒΗΝΕΙ: προτιμήσεις χωρίς προσωπικά δεδομένα (θέμα, μέγεθος κειμένου,
// ποιες καρτέλες έχεις δει) και τις «αναμνήσεις» του βοηθού, που είναι ρητή
// επιλογή του χρήστη, κλειδωμένες στο δικό του id και σβήνονται από τις
// Ρυθμίσεις. Ο καθαρισμός στην αποσύνδεση δεν είναι τιμωρία.
// ═══════════════════════════════════════════════════════════════════════════

/** Προθέματα κλειδιών που περιέχουν προσωπικά δεδομένα και σβήνονται στο logout. */
export const PERSONAL_KEY_PREFIXES = [
  'pa_hist_',          // συνομιλίες βοηθού: ονόματα ενοικιαστών, ΑΦΜ, ποσά
  'po_owner_split_',   // κατανομή συνιδιοκτητών: ονοματεπώνυμα και ΑΦΜ
] as const;

/** Σβήνει από τη συσκευή ό,τι τοπικό περιέχει προσωπικά δεδομένα.
 *  Επιστρέφει πόσα κλειδιά αφαιρέθηκαν (χρήσιμο σε τεστ και σε logs). */
export function clearLocalPersonalData(storage?: Pick<Storage, 'length' | 'key' | 'removeItem'>): number {
  const store = storage ?? (typeof localStorage === 'undefined' ? null : localStorage);
  if (!store) return 0;
  try {
    // Μαζεύουμε ΠΡΩΤΑ και σβήνουμε μετά: η αφαίρεση κατά τη διάσχιση
    // μετακινεί τους δείκτες και θα προσπερνούσε κλειδιά.
    const doomed: string[] = [];
    for (let i = 0; i < store.length; i++) {
      const k = store.key(i);
      if (k && PERSONAL_KEY_PREFIXES.some(p => k.startsWith(p))) doomed.push(k);
    }
    doomed.forEach(k => store.removeItem(k));
    return doomed.length;
  } catch {
    return 0;   // private mode ή κλειδωμένο storage
  }
}

/** Το μήνυμα που λέει στον service worker να πετάξει τις caches του. Το ίδιο
 *  ακριβώς κείμενο ακούει το public/sw.js. */
export const CACHE_CLEAR_MESSAGE = 'pos-clear-caches';

/**
 * Η ΕΞΟΔΟΣ ΑΠΟ ΤΗ ΣΥΣΚΕΥΗ, ΟΛΟΚΛΗΡΗ, ΣΕ ΜΙΑ ΚΛΗΣΗ.
 *
 * ΤΟ ΣΦΑΛΜΑ ΠΟΥ ΤΗ ΓΕΝΝΗΣΕ: ο καθαρισμός ΕΓΡΑΦΟΤΑΝ σε ένα σημείο, αλλά
 * ΚΑΛΟΥΝΤΑΝ σε ένα μόνο από τα πέντε. Πέντε δρόμοι βγάζουν τον χρήστη έξω και
 * μόνο η απλή «Αποσύνδεση» καθάριζε τη συσκευή:
 *
 *   ✓ Επισκόπηση → Αποσύνδεση
 *   ✗ Ασφάλεια → «Αποσύνδεση από όλες τις συσκευές», ο ΙΣΧΥΡΟΤΕΡΟΣ διακόπτης
 *   ✗ Ρυθμίσεις → Οριστική διαγραφή λογαριασμού
 *   ✗ /login → «είσαι ήδη συνδεδεμένος ως …, αποσύνδεση»
 *   ✗ /signup → το ίδιο
 *
 * Δηλαδή σε κοινό υπολογιστή ο χρήστης πατούσε τον πιο δυνατό διακόπτη που
 * υπάρχει και τα `pa_hist_*` (ονόματα ενοικιαστών, ΑΦΜ, ποσά μέσα σε συνομιλίες
 * του βοηθού) με τα `po_owner_split_*` (ονοματεπώνυμα και ΑΦΜ συνιδιοκτητών)
 * έμεναν στο μηχάνημα. Και μετά τη ΔΙΑΓΡΑΦΗ του λογαριασμού, ο διακομιστής
 * έσβηνε τα πάντα ενώ ο περιηγητής τα κρατούσε: προσωπικά δεδομένα τρίτων που
 * επιβίωναν του ίδιου του λογαριασμού.
 *
 * Καμία οθόνη δεν ξαναγράφει τα δύο βήματα ξεχωριστά: υπάρχει φύλακας.
 */
export function leaveDevice(deps?: {
  storage?: Pick<Storage, 'length' | 'key' | 'removeItem'>;
  /** `null` σημαίνει «κανένας worker». Παραλείποντάς το, βρίσκεται μόνος του. */
  controller?: { postMessage: (m: string) => void } | null;
}): number {
  const cleared = clearLocalPersonalData(deps?.storage);
  const controller = deps && 'controller' in deps
    ? deps.controller
    : (typeof navigator === 'undefined' ? null : navigator.serviceWorker?.controller ?? null);
  try { controller?.postMessage(CACHE_CLEAR_MESSAGE); } catch { /* κλειδωμένος ή νεκρός worker */ }
  return cleared;
}
